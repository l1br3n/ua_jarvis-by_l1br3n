# 🤖 UA Jarvis (by l1br3n)

Персональний голосовий асистент з українським інтерфейсом, побудований на базі Vosk для розпізнавання мови та інтегрований зі штучним інтелектом Google Gemini.

## ✨ Останні оновлення

* **Веб-інтерфейс**: Добавлено локальний інтерфейс для Джарвіса.

**Попередні зміни:**
* **Веб-інтерфейс для налаштувань (Flask)**: Додано локальний сайт для зручного керування конфігурацією Джарвіса. Тепер змінювати параметри роботи асистента можна через зрозумілий та приємний графічний інтерфейс у браузері (HTML/CSS), без необхідності вручну редагувати конфігураційні файли.
* **Динамічна озвучка плейлистів**: Тепер не потрібно лізти в код, щоб додати унікальний звук для плейлиста Spotify — це робиться в пару кліків прямо через веб-менеджер.

---

## 🚀 Основні можливості
* **Керування ПК**: Закриття та згортання вікон, вставка тексту з буфера обміну, керування гучністю та мікрофоном.
* **Мультимедіа та Spotify**: Запуск персональних плейлистів, перемикання треків та пошук пісень через Spotify API (Див. [короткий відео-туторіал як їх отримати](https://youtu.be/0fhkkkRuUxw?si=U74GcDt8seGxgGlW)). Звуки для кожного плейлиста зручно додаються через веб-інтерфейс.
* **Запуск програм та ігор**: Відкриття софту через налаштовані псевдоніми (аліаси) та запуск ігор у Steam.
* **Інтернет-пошук**: Швидкий пошук інформації в Google, відкриття сайтів та перегляд відео на YouTube за голосовим запитом.
* **Комунікація**: Надсилання повідомлень у Telegram контактам із вашої бази та підключення до голосових каналів Discord. (Також потрібно добавити озвучку на конкретний канал в файлі `communication.py`.)
* **Робота з клавіатурою**: Вводити текст який він почує та використовувати гарячі клавіші.
* **Можливість спілкування з потужною LLM**: Інтегрований Gemini-2.5-flash, який відповідає в стилі Джарвіса з фільму (Працює не кожен раз, скоріше за все через те що може неправильно розпізнати запит).
* **Для розробників**: Швидке відкриття робочих проєктів у Visual Studio Code (в цій версії можливі помилки).

---

## ⚙️ Налаштування

### 1. Встановлення бібліотек
Переконайтеся, що у вас встановлений Python (рекомендовано версію 3.10 або вище). Встановіть необхідні залежності:
`pip install -r requirements.txt`

### 2. Конфігурація (`config.json` та `user_data.json`)
Для повноцінної роботи потрібно створити файли конфігурації на основі прикладів:

* **ОБОВ'ЯЗКОВО**: Перейменуйте файли `config.example.json` та `user_data.example.json` на `config.json` та `user_data.json`.

### Налаштування через веб-інтерфейс
Щоб відкрити панель налаштувань у браузері, запустіть файл веб-сервера (наприклад, `app.py`) або просто запустіть Джарвіса та перейдіть за локальним посиланням (`http://127.0.0.1:5000`). Тут ви зможете зручно налаштувати:
* API-ключі для Gemini та Spotify.
* Посилання на плейлисти та файли озвучки до них.(**Обовязково** добавте файли в папку ```Sounds``` з такою ж назвою)
* Шляхи до програм, проєктів та еліаси (синоніми).
* Контакти Telegram та канали Discord.

* **Озвучка(gemini)**: Коротка настройка прописана в функції `speak_neural` в файлі `speaker.py`.
* **Відкриття головного файлу**: В файлі `open.py` можна вказати шлях до файлу `main.py`.

### 3. Встановлення мовної моделі Vosk (ОБОВ'ЯЗКОВО)
Асистент використовує локальну нейромережу Vosk для розпізнавання голосу (працює без інтернету). 
1. Перейдіть на офіційний сайт Vosk: [Vosk Models](https://alphacephei.com/vosk/models)
2. Знайдіть у списку **Ukrainian** (українську) модель.
3. Завантажте архів.
4. Розпакуйте архів у головну папку цього проєкту.
5. **Важливо:** Перейменуйте розпаковану папку на `model`. 

---

## 🛠 Запуск
Для коректної роботи всіх шляхів рекомендується використовувати лаунчер файл:
`open.py`

---

## ⚠️ Примітки для Windows
* Асистент оптимізований для Windows 11.
* Якщо виникли проблеми з мікрофоном, перевірте дозволи в налаштуваннях конфіденційності Windows.